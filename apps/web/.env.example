# URL the Next.js rewrite proxies /api/* requests to (the FastAPI backend).
API_PROXY_URL=http://localhost:8000

# Public base URL used by the browser-side API client.
NEXT_PUBLIC_API_BASE_URL=/api/v1
