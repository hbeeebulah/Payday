# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation & settings
pydantic[email]==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.1

# Database / ORM
SQLAlchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async PostgreSQL driver (production)
aiosqlite==0.20.0        # async SQLite driver (local dev / tests)
greenlet==3.1.1

# HTTP client (ALATPay integration)
httpx==0.28.1

# Cryptography (Wema Merchant Payout AES/CBC payload encryption)
cryptography==44.0.0

# Utilities
python-dotenv==1.0.1
